<?xml version="1.0" encoding="utf-8"?>
<!--
    item_sheet_option.xml
    A single tappable card inside the bottom sheet 2×2 grid.

    Wire up in PickerBottomSheetFragment like:
        ItemSheetOptionBinding.bind(binding.optionCamera).apply {
            ivSheetIcon.setImageResource(R.drawable.ic_camera)
            ivSheetIcon.imageTintList = ColorStateList.valueOf(color(R.color.icon_tint_blue))
            cvSheetIconBg.setCardBackgroundColor(color(R.color.icon_bg_blue))
            tvSheetLabel.setText(R.string.option_camera)
            root.setOnClickListener { listener.onCameraSelected() }
        }
-->
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:cardCornerRadius="16dp"
    app:cardElevation="0dp"
    app:strokeWidth="1dp"
    app:strokeColor="?attr/colorOutlineVariant"
    app:cardBackgroundColor="?attr/colorSurface"
    android:clickable="true"
    android:focusable="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:paddingVertical="20dp"
        android:paddingHorizontal="12dp">

        <!-- Icon in tonal rounded container -->
        <com.google.android.material.card.MaterialCardView
            android:id="@+id/cvSheetIconBg"
            android:layout_width="52dp"
            android:layout_height="52dp"
            app:cardCornerRadius="14dp"
            app:cardElevation="0dp"
            app:strokeWidth="0dp"
            tools:cardBackgroundColor="#E6F1FB">

            <ImageView
                android:id="@+id/ivSheetIcon"
                android:layout_width="26dp"
                android:layout_height="26dp"
                android:layout_gravity="center"
                android:importantForAccessibility="no"
                tools:src="@drawable/ic_camera"
                tools:tint="#185FA5" />

        </com.google.android.material.card.MaterialCardView>

        <!-- Label -->
        <TextView
            android:id="@+id/tvSheetLabel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            tools:text="Camera"
            style="@style/TextAppearance.Material3.LabelLarge"
            android:textColor="?attr/colorOnSurface" />

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>